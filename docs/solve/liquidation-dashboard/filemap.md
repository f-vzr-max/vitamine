# Filemap — liquidation-dashboard

| path | role | key symbols | ~lines |
|------|------|-------------|--------|
| `package.json` | deps + scripts, EXACT pins: `next@16.3.5`, `next-auth@5.0.0-beta.32`, `prisma@7.10.0`, `@prisma/client@7.10.0`, `@prisma/adapter-pg`, `pg`, `bcryptjs`, `vitest@5.0.1`, `tsx`, `@types/node@^22` | `scripts.build`, `scripts.start`, `scripts.test` | ~55 |
| `package-lock.json` | committed — `npm ci` (C1) refuses to run without it; `.gitignore` must not exclude it | — | (generated) |
| `tsconfig.json` | TS config | — | ~25 |
| `next.config.ts` | Next config | — | ~15 |
| `prisma.config.ts` | Prisma 7.x config — datasource URL + `@prisma/adapter-pg` driver adapter wiring (client 7.x moved this out of `schema.prisma`) | — | ~30 |
| `.env.example` | documents `DATABASE_URL`, `AUTH_SECRET`, `AUTH_URL`, `AUTH_TRUST_HOST` (Auth.js v5 names, not v4 `NEXTAUTH_*`; no real values) | — | ~12 |
| `.gitignore` | excludes `node_modules`, `.next`, `.env` — NOT `package-lock.json` | — | ~15 |
| `vitest.config.ts` | test runner config for `tests/wiring/*` | — | ~15 |
| `prisma/schema.prisma` | full schema: `User, Article, PressLog, Sale, Client, Employee, ShiftAssignment, LiquidationConfig`; `LiquidationConfig` carries both `vracBaselineBales` and `prePressedStockBales` | `model User` … `model LiquidationConfig` | ~150 |
| `prisma/seed.ts` | deterministic fixtures for dev + wiring checks (dev/test only — real accounts come from `scripts/create-user.ts`, never seeded passwords) | `main()` | ~80 |
| `prisma/migrations/<ts>_init/migration.sql` | generated by `prisma migrate dev` | — | ~150 (generated) |
| `scripts/create-user.ts` | creates one real account (`npx tsx scripts/create-user.ts --email … --name … --password-env PW`) — the ONLY path real passwords take; also used by C5's positive-login check | `main()` | ~40 |
| `src/proxy.ts` | Next 16 route guard (renamed from `middleware.ts`, Node runtime) — redirects unauth to `/login`, 401s unauth API writes (C5), matcher excludes `/api/auth/*` (A7) | `default export` | ~40 |
| `src/lib/db.ts` | Prisma client singleton | `prisma` | ~15 |
| `src/lib/auth.ts` | Auth.js v5 Credentials provider, bcrypt compare, JWT session (`maxAge` 60-90d, `updateAge` sliding), `trustHost: true` — single mockable seam (A8) | `authConfig`, `auth()` | ~65 |
| `src/lib/targets.ts` | phase-1/phase-2 weekly cumulative % tables, keyed by week-of-campaign from a given date | `PHASE1_TARGETS`, `PHASE2_TARGETS`, `weekOf(date)` | ~45 |
| `src/lib/metrics.ts` | pure computation, all take an `asOf` param (G3): pressed/sold %, `soldPct` denominator = `prePressedStockBales + ΣPressLog.qty`, days remaining, target %, rest compliance + daily coverage, cash recon by day/week | `computePressedPct`, `computeSoldPct`, `computeDaysRemaining`, `computeTargets`, `computeRestCompliance`, `computeCashReconciliation` | ~140 |
| `src/app/layout.tsx` | root layout + nav | `RootLayout` | ~50 |
| `src/app/page.tsx` | home dashboard (req 7) — reads `/api/dashboard-summary` | `Page` | ~120 |
| `src/app/login/page.tsx` | credential login form (MANUAL — look-and-feel) | `Page` | ~70 |
| `src/app/articles/page.tsx` | editable article/price/stock catalog (req 2) | `Page` | ~90 |
| `src/app/press-log/page.tsx` | phase-1 daily bales-pressed entry (req 3) | `Page` | ~90 |
| `src/app/sales/page.tsx` | phase-2 daily bales-sold entry (req 3) | `Page` | ~100 |
| `src/app/clients/page.tsx` | client directory + order history (req 4) | `Page` | ~80 |
| `src/app/cash/page.tsx` | cash reconciliation view (req 5) | `Page` | ~70 |
| `src/app/team/page.tsx` | roster + rest-day indicator (req 6) | `Page` | ~80 |
| `src/app/api/auth/[...nextauth]/route.ts` | NextAuth handlers | `GET`, `POST` | ~15 |
| `src/app/api/articles/route.ts` | catalog list/create | `GET`, `POST` | ~60 |
| `src/app/api/articles/[id]/route.ts` | catalog price/stock update | `PATCH` | ~40 |
| `src/app/api/press-log/route.ts` | phase-1 daily log (C4) | `GET`, `POST` | ~60 |
| `src/app/api/sales/route.ts` | phase-2 daily log — computes `revenueAr` server-side, requires `articleId`+`clientId`, decrements `Article.stock` in the same transaction (C3, G6) | `GET`, `POST` | ~85 |
| `src/app/api/clients/route.ts` | client directory list/create | `GET`, `POST` | ~50 |
| `src/app/api/clients/[id]/route.ts` | client detail + sale history | `GET` | ~40 |
| `src/app/api/employees/route.ts` | roster CRUD | `GET`, `POST` | ~50 |
| `src/app/api/config/baseline/route.ts` | get/set `LiquidationConfig.vracBaselineBales` + `prePressedStockBales` (C4, G1) | `GET`, `PUT` | ~40 |
| `src/app/api/dashboard-summary/route.ts` | live aggregate JSON, `?asOf=YYYY-MM-DD` (default today) — pressed/sold %, target %, days remaining, revenue-to-date (C3, C4, G2, G3) | `GET` | ~80 |
| `src/app/api/cash-reconciliation/route.ts` | revenue totals `byDay` AND `byWeek` (C6, G5) | `GET` | ~55 |
| `src/app/api/team/rest-check/route.ts` | per-employee rest compliance + per-day coverage flag (C7, G4) | `GET` | ~55 |
| `tests/wiring/sales.test.ts` | drives C3 | `test('sales wiring...')` | ~60 |
| `tests/wiring/press-log.test.ts` | drives C4 | `test('press baseline...')` | ~60 |
| `tests/wiring/cash-reconciliation.test.ts` | drives C6 | `test('cash recon...')` | ~50 |
| `tests/wiring/roster-rest.test.ts` | drives C7 | `test('rest compliance...')` | ~60 |
| `tests/unit/metrics.test.ts` | pure-function coverage for `src/lib/metrics.ts` | — | ~60 |
| `scripts/checks/_pg.sh` | shared lib, sourced by every check below — ephemeral `postgres:16` (cached image, not `-alpine`), random host port (`-p 127.0.0.1::5432` + `docker port`), unique container name `vt-$$`, `pg_isready` poll (30s deadline), `prisma migrate deploy`, `trap 'docker rm -f …' EXIT` | — | ~40 |
| `scripts/checks/schema.sh` | C2 | — | ~20 |
| `scripts/checks/sales-wiring.sh` | wraps sales.test.ts (C3) | — | ~20 |
| `scripts/checks/press-wiring.sh` | wraps press-log.test.ts (C4) | — | ~20 |
| `scripts/checks/auth-guard.sh` | C5 — starts server on existing build (never rebuilds), `AUTH_SECRET`+`AUTH_TRUST_HOST=true`, negative curl + positive create-user/login/POST flow | — | ~40 |
| `scripts/checks/cash-reconciliation.sh` | wraps cash-reconciliation.test.ts (C6) | — | ~20 |
| `scripts/checks/roster-rest.sh` | wraps roster-rest.test.ts (C7) | — | ~20 |
| `docs/reference/plan-vidage-depot.html` | existing — source business plan, read-only reference | — | 454 (existing) |
