# Build log — liquidation-dashboard

Append-only. UTC+3.

- 2026-09-16T16:44:22+03:00 start. Read criteria.md, filemap.md, validation-scope.md, plan-vidage-depot.html. Beginning scaffold.
- 2026-09-16T16:47:00+03:00 package.json, tsconfig.json, next.config.ts, .gitignore written. DEVIATION: `.env.example` write blocked by permission deny-rule (Write tool refuses the path outright, even placeholder-only content) — CLAUDE.md traps confirm this is by-design ("`.env` / `.env.example` writes are deny-ruled. Do not work around any of it"). Skipping that file; required env vars (`DATABASE_URL`, `AUTH_SECRET`, `AUTH_URL`, `AUTH_TRUST_HOST`) documented here instead. Not gate-blocking — no C1-C7 row reads .env.example.
- 2026-09-16T17:05:00+03:00 npm install done (49s), all exact pins confirmed via `npm ls`. Probed Prisma 7.10.0 config shape in scratchpad against an ephemeral postgres:16 container: `datasource db { url = env(...) }` in schema.prisma is REMOVED in Prisma 7 (P1012 validation error) — URL now lives only in `prisma.config.ts`'s `datasource.url`, PrismaClient requires `adapter` (confirmed "driver adapter is **required**" in generated .d.ts). Used classic `generator client { provider = "prisma-client-js" }` (not the new `"prisma-client"` generator with explicit `output`) — generates into `node_modules/@prisma/client`, importable as plain `from "@prisma/client"`, same adapter requirement, simpler than the new ESM-.ts-source generator. `prisma.config.ts` (filemap's legacy-discovered name, not the new default `prisma7.config.ts`) confirmed working for both `migrate dev` and `migrate deploy` end-to-end against ephemeral PG. `new PrismaPg({ connectionString: process.env.DATABASE_URL })` + `new PrismaClient({ adapter })` confirmed create+findMany round-trip.
- 2026-09-16T17:15:00+03:00 DEVIATION from filemap: `PressLog.date`/`Sale.date`/`ShiftAssignment.date` are `String` ("YYYY-MM-DD"), not `DateTime @db.Date`. Host TZ is Europe/Berlin (UTC+2, confirmed via `timedatectl`); pg's default DATE parser returns local-midnight JS Date objects, and `.toISOString().slice(0,10)` on that shifts the day backward — a real off-by-one bug for calendar-date business fields. String columns sidestep it entirely; `src/lib/targets.ts`'s `parseDateUTC` treats all date strings as UTC-midnight consistently. Regenerated `prisma/migrations/20260916135456_init/` after this change (old one deleted, never committed).
- 2026-09-16T17:20:00+03:00 schema.prisma (8 models) + prisma.config.ts + initial migration done, verified end-to-end against ephemeral PG (migrate dev + migrate deploy both work).
- 2026-09-16T17:45:00+03:00 src/lib/db.ts, targets.ts, metrics.ts, auth.ts, src/proxy.ts, scripts/create-user.ts, src/app/api/auth/[...nextauth]/route.ts done. All 11 API routes done (articles, articles/[id], press-log, sales, clients, clients/[id], employees, config/baseline, dashboard-summary, cash-reconciliation, team/rest-check). Sale POST wraps stock decrement + insert in one `$transaction` (C3/G6). dashboard-summary upserts a singleton LiquidationConfig row so it never 500s on an empty DB.
- 2026-09-16T18:00:00+03:00 All 7 pages written (layout, home dashboard, login, articles, press-log, sales, clients, cash, team) + globals.css, French copy, mobile-first (nav scrolls horizontally on narrow screens, forms stack full-width, 16px inputs to avoid iOS zoom). Client components fetching the same API routes the checks exercise (A11).
- 2026-09-16T18:05:00+03:00 First `npm run build` failed: "Route segment config is not allowed in Proxy file" — `export const runtime = "nodejs"` in src/proxy.ts is redundant/forbidden, proxy.ts is ALWAYS Node runtime by construction in Next 16. Removed it. Also pinned `turbopack.root` in next.config.ts (`import.meta.dirname`) — Turbopack was walking up past the repo root into the OUTER executive-assistant repo's unrelated package-lock.json and warning about it. Build then green: 19 routes, ~13s warm.
- 2026-09-16T18:10:00+03:00 Timed the exact C1 cell (`npm ci && npm run build && test -f .next/BUILD_ID`): 41.8s real. Well under the 120s row cap.
- 2026-09-16T18:15:00+03:00 scripts/checks/_pg.sh + all 6 check scripts (schema, sales-wiring, press-wiring, auth-guard, cash-reconciliation, roster-rest) written, plus tests/wiring/*.test.ts (4 files) and tests/unit/metrics.test.ts. Bug found+fixed in schema.sh: initial table-count query included Prisma's own `_prisma_migrations` bookkeeping table (9 found, not 8) — excluded it explicitly.
- 2026-09-16T18:20:00+03:00 C1-C7 run individually: all 7 GREEN on first attempt (schema.sh fix above was the only iteration needed).
- 2026-09-16T18:25:00+03:00 Per operator rule (resolve twice, re-run before declaring done): re-ran the FULL sequence C1->C7 end to end from a clean `.next` a second time. All 7 still PASS. No orphaned `vt-*` docker containers left behind (trap cleanup verified via `docker ps -a`). ACCEPTANCE GATE MET.
- 2026-09-16T18:30:00+03:00 `npm audit`: 4 high-severity findings, all inside `prisma`'s own devDependency tree (`mysql2`, `deepmerge-ts` — Prisma's MySQL-connector code path, which this Postgres-only app never exercises). No fix available without `prisma@latest` (8.0.0-rc), which criteria.md explicitly forbids ("not latest"). Accepted, non-blocking, noted here per the pin constraint.
- 2026-09-16T18:35:00+03:00 Line-count self-audit: largest authored file is 144 lines (src/app/sales/page.tsx); everything else well under the 500-line cap. No console.log/TODO/FIXME left in src/scripts/prisma except the two intentional CLI-output lines in create-user.ts/seed.ts.
- 2026-09-16T18:40:00+03:00 Ran the ONE production migration: `set -a && . .secrets/db-local.conf && set +a && npx prisma migrate deploy` against the persistent `vitamine-postgres` container (127.0.0.1:5434, db `vitamine`). Succeeded, migration `20260916135456_init` applied. Confirmed target host/port/db name via `node -e` (no credentials echoed).

